# Временный лог разработки (Temporary Dev Log)

> **Примечание:** Данный файл является временным и используется для фиксации текущего прогресса, обсужденных формул и планирования следующих шагов. После завершения этапа разработки логики данные будут перенесены в основную документацию.

> **Примечание:** '!!!' - так я обозначаю свои комментарии для тебя. в нутри комментария оставлю пояснение что с ним делать, либо выполнить и удалить либо оставить
> 
## 📅 Текущий статус
**Этап:** Реализована функция редактирования операций. Оптимизирован интерфейс ввода (устранены обрезания текста, улучшена компактность). Система готова к финальному оформлению.

---

## 🏗️ Что уже сделано
1.  **Архитектура**: Создана структура папок проекта, соблюден принцип SoC.
2.  **Модели данных**: В `core/operations.py` описаны классы для всех типов операций.
3.  **Конфигурация**: Создан `machines_config.json` с параметрами станков.
4.  **Загрузчик**: Реализован `utils/config_loader.py`.
5.  **Базовые расчеты**: Реализован `core/base_calculator.py` с учетом коэффициентов холостого хода (1.2 и 1.3).
6.  **Управление сессией**: Создан `core/session_manager.py` для хранения промежуточных результатов. Реализована поддержка двух каналов (Main/Back Spindle).
7.  **Специфические расчеты**: Реализован `core/specific_calc.py` с логикой многопроходности и спецификой операций (Facing, Turning, Milling и т.д.).
8.  **Экспорт отчетов**: Реализован `utils/file_exporter.py`. Введена сущность `ReportGenerator` для формирования текста и `FileExporter` для записи в `.txt`.
9.  **Графический интерфейс (GUI)**:
    - Реализовано главное окно (`gui/app.py`) с двухпанельным интерфейсом (Main и Back Spindle одновременно).
    - Реализованы фреймы для Main Spindle и Back Spindle с динамическими полями ввода для всех 7 типов операций.
    - Интегрирована система уведомлений об ошибках ввода (статус-бары).
    - Интегрирована кнопка генерации отчета с автоматическим расчетом времени цикла (Max + Overhead).
    - **Обновление UI**: Поля ввода теперь размещены компактно в ряд с фиксированной шириной, добавлено поле "Комментарий", список операций стал более информативным (параметры + комментарии) и визуально выделенным. Окна потоков зафиксированы 50/50.
10. **Контроль версий**: Проект инициализирован как Git-репозиторий, настроен `.gitignore`, создана ветка `dev` и выполнен Push на GitHub.
11. **Исправление ошибок и уточнение логики**:
    - Исправлен баг "Select machine first": теперь первый станок из конфига выбирается автоматически при запуске.
    - Уточнена формула для **Drilling (Q)**: теперь расчет учитывает время на множественные подводы и отводы инструмента (ретракты).
    - **Обновление списка операций**: Комментарии теперь отображаются выше параметров и корректно переносятся на новую строку (до 2-х строк), что предотвращает их обрезание.
12. **Финальное тестирование и багфиксы**:
    - Исправлен баг загрузки иконок для операций "Threading" и "Whirling" (устранены опечатки в маппинге).
    - Улучшено форматирование параметров в текстовом отчете (использование компактного вывода чисел).
    - Проверена корректность расчетов для всех типов операций через автоматизированный скрипт `reproduce_issue.py`.
13. **Функция импорта проекта (Open Project)**:
    - Реализован механизм «обратного парсинга» .txt отчетов (`utils/report_parser.py`).
    - Централизованы метки параметров (`utils/labels.py`) для обеспечения 100% совместимости при сохранении/загрузке.
    - В UI добавлена кнопка «Открыть проект», позволяющая восстановить состояние сессии, включая выбор станка, имя проекта и списки всех операций с комментариями.
    - Проведен успешный интеграционный тест цикла «Сохранение -> Чтение -> Восстановление».
14. **Исправление критических багов**:
    - Исправлена ошибка `AttributeError: 'SessionManager' object has no attribute 'results'` при открытии проекта. Теперь используется метод `clear_session()`.
## 🏗️ Что уже сделано
...
15. **Функция редактирования операций**:
    - В `SessionManager` добавлен метод `update_result` для изменения параметров существующей записи без потери порядка.
    - В UI реализована возможность клика по операции в списке для загрузки её данных обратно в поля ввода.
    - Кнопка "Calculate" динамически меняется на "Update" при редактировании, подсвечивая активную операцию в списке.
16. **Рефакторинг путей (pathlib)**:
    - Все операции с файловой системой (загрузка конфигов, сохранение отчетов, чтение иконок) переведены на использование `pathlib.Path`.
    - Обеспечена 100% совместимость путей между macOS и Windows без использования ручных слэшей.


## 📂 Актуальная структура проекта
```text
/calculator
├── main.py                # Точка входа (запуск приложения)
├── GEMINI.md              # Основные правила и стандарты разработки
├── DEV_LOG.md             # Лог разработки, математика и структура (этот файл)
├── machines_config.json   # JSON-конфигурация параметров станков
├── requirements.txt       # Список зависимостей (customtkinter и др.)
├── .gitignore             # Исключения для Git
├── core/                  # Бизнес-логика и расчеты
│   ├── __init__.py
│   ├── base_calculator.py # Базовые формулы времени (Т_раб + Т_хол)
│   ├── operations.py      # Data-классы входных параметров операций
│   ├── session_manager.py # Хранение результатов (Main/Back каналы)
│   └── specific_calc.py   # Специфическая логика для конкретных операций
├── gui/                   # Графический интерфейс
│   ├── __init__.py
│   ├── app.py             # Главное окно и навигация (Двухпанельный режим)
│   ├── main_spindle.py    # UI вкладки основного шпинделя
│   └── back_spindle.py    # UI вкладки противошпинделя
├── utils/                 # Вспомогательные инструменты
│   ├── __init__.py
│   ├── config_loader.py   # Безопасная загрузка JSON-конфигураций
│   ├── file_exporter.py   # Генерация .txt отчетов
│   ├── report_parser.py   # Парсинг .txt для импорта
│   └── labels.py          # Единые метки параметров
├── output/                # Папка для сохранения готовых отчетов
├── assets/                # Иконки и стилистические ресурсы
└── context/               # Обучающие материалы по Python (справочно)
```

---

## 🛠️ Правила работы с этим файлом (Workflow)
- **Обсуждение**: Вопросы и комментарии к нереализованным пунктам пишутся непосредственно над ними.
- **Реализация**: Как только пункт реализован, комментарий к нему удаляется, а сам пункт переносится из "Планируемого" или "В процессе" в раздел "Что уже сделано".

---

## 🧪 Математический фундамент (В процессе обсуждения)

### Общие формулы:
- [x] **Время точения/сверления (T):** `T = L / (S * F) + (1.3 * L) / Rapid_Traverse`
- [x] **Время фрезерования (T):** `T = L / F_min + (1.2 * L) / Rapid_Traverse`


### Вопросы для уточнения:
1.  Нужно ли учитывать время на разгон/торможение шпинделя? !!! нет, не нужно (пока что не учитываем, но не удаляй этот пункт)
2.  Должен ли базовый класс хранить "накопленное" общее время или считать только одну операцию? (Реализовано через SessionManager).

---

## 🚀 Планируемые шаги
[Завершено] !!!Поскольку ты тестируешь код на Mac, а работать он будет на Windows, следи за путями к файлам. Твои JSON-конфиги станков и TXT-отчеты должны сохраняться корректно на обеих системах.
[Завершено] !!!Никогда не пиши пути через слэши вручную (на Mac это /, на Windows это \). Всегда используй встроенный модуль pathlib
1.  **[Завершено] Шаг 1:** Финальное тестирование всех операций.
2.  **[Завершено] Шаг 2:** Функция импорта/открытия проекта из .txt.
3.  **[Завершено] Шаг 3:** Оформление дизайна (assets, иконки, цвета).
4.  **[Завершено] Шаг 4:** Подготовка документации для пользователя.

---
*Последнее обновление: 2026-05-23*
